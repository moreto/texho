import 'package:get_it/get_it.dart';

final GetIt locator = GetIt.instance;

void setupLocator() {
  // locator.registerFactory<BancoRepository>(() => BancoRepositoryImpl());
  // locator.registerFactory<ContaRepository>(() => ContaRepositoryImpl());
}